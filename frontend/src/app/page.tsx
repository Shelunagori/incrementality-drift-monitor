/** Placeholder dashboard page; replaced by the traffic-light dashboard in Phase 5. */
export default function HomePage() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-semibold">Incrementality Drift Monitor</h1>
      <p className="mt-2 text-slate-600">
        Statistics decide what happened, AI explains and proposes, a human approves.
      </p>
      <p className="mt-4 text-sm text-slate-500">All data shown is synthetic.</p>
    </main>
  );
}
